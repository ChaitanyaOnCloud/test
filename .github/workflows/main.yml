name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      # Remove or update the 'cd backend' step
      # If backend directory does not exist, skip this step
      # - name: Set up Python
      #   uses: actions/setup-python@v4
      #   with:
      #     python-version: '3.10'
      # - name: Install dependencies
      #   run: |
      #     cd backend
      #     pip install -r requirements.txt
      # - name: Run tests
      #   run: |
      #     cd backend
      #     pytest
      # If backend directory is not present, above steps are commented out.
      # Add any frontend steps as needed below.
      - name: Frontend Check
        run: echo "Frontend build step (if any)"
